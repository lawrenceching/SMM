name: E2E Tests for AI Tools

# Placeholder for e2e against the built-in AI Agent chat window
# (see docs/dev/supported-platform.md — AI tool).
# Specs will live under apps/e2e/test/specs/ai/*.e2e.ts.
# Manual-only; not a CI or release gate.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  e2e-ai:
    name: E2E Tests for AI Tools (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "E2E Tests for AI Tools is not implemented yet."
          echo "Planned specs: apps/e2e/test/specs/ai/*.e2e.ts"
          echo "See docs/dev/supported-platform.md (AI tool)."
