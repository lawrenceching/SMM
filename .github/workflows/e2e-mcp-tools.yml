name: E2E Tests for MCP Tools

# Placeholder for e2e against external AI agents talking to SMM via MCP
# (see docs/dev/supported-platform.md — MCP tool).
# Specs will live under apps/e2e/common/mcp/*.e2e.ts (and/or apps/e2e/test/mcp/).
# Manual-only; not a CI or release gate.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  e2e-mcp:
    name: E2E Tests for MCP Tools (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "E2E Tests for MCP Tools is not implemented yet."
          echo "Planned specs: apps/e2e/common/mcp/*.e2e.ts"
          echo "See docs/dev/supported-platform.md (MCP tool)."
