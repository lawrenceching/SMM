# Migrate to TanStack Query and Zustand

UIMediaMetadata was splitted into UIMediaFolder and MediaMetadata

UIMediaFolder is UI state, managed by `apps\ui\src\stores\uiMediaFolderStore.ts`
and MediaMetadata comes from backend, managed by `apps\ui\src\hooks\mediaMetadata`


